{
  "name": "mnexus-frontend-tests",
  "version": "2.6.0",
  "description": "Unit tests for M-NEXUS frontend (vanilla JS + CSS)",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^2.1.9",
    "@vitest/coverage-v8": "^2.1.9",
    "jsdom": "^25.0.1",
    "@testing-library/dom": "^10.4.0"
  }
}
