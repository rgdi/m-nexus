# M-NEXUS release keystore configuration.
# v0.32: this is the SINGLE keystore used to sign all M-NEXUS companion APKs.
# 
# IMPORTANT: this keystore MUST be the same across all releases. If you
# change it, all existing users will see the new APK as a NEW app
# (Android won't recognize the certificate).
# 
# If you need to rotate it, follow the official migration guide:
# https://developer.android.com/about/versions/pie/android-9.0-changes-28#package-signing
#
# Keep this file safe (and the keystore). The password here is the
# same as the keystore password, which is needed for the build.

storePassword=mnexus2024
keyPassword=mnexus2024
keyAlias=mnexus
storeFile=keystores/mnexus-release.keystore
