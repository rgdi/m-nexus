<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- v2.19.0: intent for REQUEST_IGNORE_BATTERY_OPTIMIZATIONS dialog.
             The JS layer dispatches this via the battery plugin to ask the
             user to whitelist the app from doze mode. -->
        <receiver
            android:name=".BatteryOptimizationReceiver"
            android:exported="false" />

        <!-- v2.19.0: foreground service for sync. Keeps the WS connection
             alive when the app is backgrounded. -->
        <service
            android:name=".SyncForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- v2.21.0: notification listener service. Requires user opt-in
             via Settings → Notifications → Device & app notifications →
             M-NEXUS. Once granted, we capture metadata (app, title, text,
             category, when) and forward to the backend as events.
             Read-only: we do not dismiss / snooze notifications. -->
        <service
            android:name=".NotificationCaptureService"
            android:label="@string/notif_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

    <!-- ============================================================== -->
    <!-- Permissions (v2.19.0: granular runtime permissions)              -->
    <!--                                                                -->
    <!-- All dangerous permissions are listed in <uses-permission>     -->
    <!-- AND must be requested at runtime via Permission.request().     -->
    <!-- The status is reported back to the backend via                  -->
    <!-- PATCH /api/v1/devices/:id/permissions so admin dashboards      -->
    <!-- can see which devices have granted what.                       -->
    <!-- ============================================================== -->

    <!-- Network (always) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Storage / filesystem (offline queue + attachments) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="29"
                     tools:ignore="ScopedStorage" />
    <!-- Android 13+ granular media perms -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Scoped storage for app's own files (no perm needed at runtime) -->

    <!-- Audio recording (audio notes / waveform / OCR training) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Camera (future: scan textbook pages, profile photo) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Location (future: geotag events/notes) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Notifications (FSRS reminders, conflict merges, sync status) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Pre-Android-13 (legacy) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Battery optimization (allow background sync + alarms) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Foreground service (keep sync alive when backgrounded) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Exact alarms (FSRS "review at 09:00" reminders, Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Boot completed (restart scheduled reminders after device reboot) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground sync types (Android 14 requires explicit type) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- v2.21.0: notification listener access. The user must enable us
         manually via Settings → Notifications. We never read personal
         message bodies; only metadata (app, title, category, when). -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />
</manifest>
